<UserControl x:Class="GraphPathfinder.Views.GraphTextInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="200">
    <Border BorderBrush="#FFF0F0F0" BorderThickness="1,0,0,0">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Vertices (ID: X,Y):" Grid.Row="0" Margin="0,0,0,4"/>
        <TextBox x:Name="VerticesInputBox" Grid.Row="1" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" MinHeight="60"/>
        <TextBlock Text="Edges (e.g. 1->2, 2--3 [w=5]):" Grid.Row="2" Margin="0,8,0,4"/>
        <TextBox x:Name="EdgesInputBox" Grid.Row="3" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" MinHeight="60"/>
        <Button x:Name="ParseGraphButton" Grid.Row="4" Content="Parse Graph" HorizontalAlignment="Right" Margin="0,12,0,0" Width="100"/>
    </Grid>
</Border>
</UserControl>
